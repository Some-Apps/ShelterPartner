import 'package:flutter/material.dart';

class VolunteerSettingsPage extends StatelessWidget {
  const VolunteerSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Volunteer Settings"),
      ),
      body: const Center(
        child: Text("Volunteer Settings"),
      ),
    );
  }
}